# 功能更新记录

## (2026-06-27) - 平台自适应字体 + 选择窗口重命名

### 修改内容

- **平台自适应默认字体**: 新增 `get_font_families()` / `get_font_family()` / `get_font_family_css()` 工具函数
  - 根据 `platform.system()` 自动判断 Windows / Linux / macOS
  - Windows: 优先使用 `Microsoft YaHei` / `Segoe UI` / `Consolas`
  - Linux: 优先使用 `Noto Sans CJK SC` / `WenQuanYi Micro Hei` / `DejaVu Sans` / `DejaVu Sans Mono`
  - macOS: 优先使用 `PingFang SC` / `SF Pro Text` / `Menlo`
  - 三种字体用途: `"ui"` 主界面/标注、`"numeric"` 数字/英文标签、`"mono"` 等宽数值

- **替换所有硬编码字体**: 将 `TAB Score Viewer.py` 中所有写死的 `Microsoft YaHei` / `Arial` / `Consolas` 替换为平台自适应函数
  - 样式表 `font-family` 改为 `get_font_family_css('ui')`
  - `QFont(...)` 绘制调用改为 `get_font_family('ui'/'numeric'/'mono')`
  - 标注数据模型 `Annotation.font_family` 默认值改为平台自适应 UI 字体

- **选择窗口重命名**: 将原 `SettingsWindow` 类改名为 `SelectionWindow`
  - 类定义、docstring、方法注释、程序入口实例化全部同步更新
  - 窗口标题翻译 `app.settings_title` 从「设置」改为「选择窗口」
  - 英文翻译同步改为 `Selection Window`

### 修改文件

| 文件 | 改动 |
| --- | --- |
| `TAB Score Viewer.py` | 新增平台自适应字体工具函数; 替换全部硬编码字体; `SettingsWindow` → `SelectionWindow`; 文件头注释更新 |
| `locales/zh_CN.json` | `app.settings_title` 改为「TAB Score Viewer - 选择窗口」 |
| `locales/en_US.json` | `app.settings_title` 改为「TAB Score Viewer - Selection Window」 |
| `readme/功能更新.md` | 新增本次更新记录; 删除最早的几条旧记录以控制文件长度 |
| `readme/开发文档.md` | 同步更新 `SelectionWindow` 相关描述 |
| `README.md` | 同步更新 `SelectionWindow` 架构说明 |
| `release notes.md` | 同步更新 `SelectionWindow` 架构说明 |

### 10条Case预期结果

| Case | 场景 | 预期结果 |
| --- | --- | --- |
| 1 | Windows 运行程序 | 界面使用 `Microsoft YaHei` / `Segoe UI` / `Consolas`, 与修改前一致 |
| 2 | Linux (Ubuntu) 运行程序 | 界面优先使用 `Noto Sans CJK SC` / `DejaVu Sans`, 中文不再缺失 |
| 3 | macOS 运行程序 | 界面优先使用 `PingFang SC` / `SF Pro Text` / `Menlo`, 风格与系统一致 |
| 4 | 启动主窗口 | 窗口标题显示「TAB Score Viewer - 选择窗口」(中文) 或「Selection Window」(英文) |
| 5 | 新建标注 | 标注默认字体为当前平台 UI 字体, 不再硬编码为 `Microsoft YaHei` |
| 6 | 导出 PNG/PDF | 页码使用平台 numeric 字体渲染, 位置与之前一致 |
| 7 | GTP 音量滑块 | dB 数值使用平台 mono 字体, 等宽对齐 |
| 8 | 切换语言 | 窗口标题随语言切换显示对应翻译 |
| 9 | 主题切换 | 所有控件样式表中的 `font-family` 使用平台自适应 CSS 列表 |
| 10 | 打包后运行(PyInstaller) | `platform.system()` 仍能正确识别系统, 字体选择正确 |

---

## (2026-06-27) - README 添加效果图与 macOS 支持文档

### 修改内容

- **README 添加效果图**: 在英文和中文部分的开头各添加两张应用截图（`renderedlight (1).png`、`renderedlight (2).png`）
  - 使用 `<p align="center">` 实现双图并排显示，各占 45% 宽度
  - 英文截图 alt 文本使用英文描述，中文截图使用中文描述

- **新增 macOS APP 打包说明**: 在 Packaging & Distribution（英文）和打包发布说明（中文）中新增 macOS 平台支持
  - macOS（APP 安装包）：使用 PyInstaller 打包为原生 `.app` 包
  - 前置条件：Xcode Command Line Tools + PyInstaller
  - 构建命令：与 Windows 共用同一 `.spec` 文件
  - 系统依赖：通过 Homebrew 或 MacPorts 安装 FluidSynth 和 libsndfile
  - macOS 特别说明：Gatekeeper 处理、codesign 签名、隔离检查绕过

- **更新打包信息**: 在多处同步更新 macOS 支持信息
  - Packaging & Distribution 标题：改为 "Windows (EXE), Linux (DEB/ZIP), and macOS (APP)"
  - Tech Stack 表格 Packaging 行：添加 macOS APP
  - 中文 跨平台打包 行：添加 macOS APP
  - Spec Config 表格：添加 macOS 使用相同 spec 文件的说明
  - 中文 spec 配置说明：添加 macOS 使用相同 spec 文件的说明

### 修改文件

| 文件 | 改动 |
| --- | --- |
| `README.md` | 英文/中文开头各添加两张效果图；新增 macOS APP 打包说明（英文+中文）；更新打包相关表格和标题；更新 Spec Config 说明 |
| 新增图片引用 | `renderedlight (1).png`、`renderedlight (2).png`（项目中已存在，无需新文件） |

### 10条Case预期结果

| Case | 场景 | 预期结果 |
| --- | --- | --- |
| 1 | 打开 README.md 英文部分 | 标题下方显示两张并排效果图，图片正确加载 |
| 2 | 打开 README.md 中文部分 | 中文标题下方显示两张并排效果图，图片正确加载 |
| 3 | README 英文 Packaging 标题 | 显示 "Windows (EXE), Linux (DEB/ZIP), and macOS (APP)" |
| 4 | 英文 Packaging 中查看 macOS 部分 | 有完整的前置条件、构建命令、运行方式、系统依赖说明 |
| 5 | macOS 用户按说明打包 | 运行 `pyinstaller "TAB Score Viewer.spec"` 生成 `.app` 包 |
| 6 | macOS 用户安装系统依赖 | `brew install fluidsynth libsndfile` 成功安装 FluidSynth |
| 7 | 英文 Tech Stack 表格 | Packaging 行显示 "Windows EXE / Linux DEB+ZIP / macOS APP" |
| 8 | 中文 跨平台打包 行 | 显示 "Windows EXE / Linux DEB+ZIP / macOS APP" |
| 9 | 英文 Spec Config 说明 | 底部有 macOS 使用相同 spec 文件的 Note 说明 |
| 10 | 中文 spec 配置说明 | 底部有 macOS 使用相同 spec 文件的注意说明 |

---

## 待规划功能
- [x] ~~GTP解析渲染~~ ✅ v1.1.0 | ~~音频播放~~ ✅ v1.3.0 | ~~技巧符号~~ ✅ v1.2.x | ~~多轨~~ ✅ v1.1.1 | ~~库化~~ ✅ v1.8.x | ~~多语言~~ ✅ v1.9.0 | ~~图标+打包~~ ✅ v1.9.1 | ~~主题系统~~ ✅ v2.0.0
- [x] 标注导入导出(已有基础) | ~~最近文件~~ ✅ | 全屏模式
